{
	"name": "www.mayer.pub",
	"version": "1.0.0",
	"description": "",
	"license": "",
	"main": "index.js",
	"scripts": {
		"dev": "next",
		"build": "next build",
		"start": "next start",
		"export": "next export && cp _config.yml out/ && cp CNAME out/ && cp .nojekyll out/",
		"deploy": "gh-pages --dotfiles -d out",
		"full-deploy": "yarn build && yarn export && yarn deploy"
	},
	"dependencies": {
		"next": "^7.0.2",
		"react": "^16.7.0",
		"react-dom": "^16.7.0",
		"prop-types": "latest"
	}
}
